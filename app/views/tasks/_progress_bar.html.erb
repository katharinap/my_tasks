<div class="progress">
  <div id="<%= id %>" class="progress-bar progress-bar-striped <%= 'bg-success' unless progress < 100 %>" role="progressbar" style="width: <%= progress %>%" aria-valuenow="<%= progress %>" aria-valuemin="0" aria-valuemax="100">
    <%= progress %>%
  </div>
</div>
